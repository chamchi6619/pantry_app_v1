{
  "name": "pantry-app-scripts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "ingest:local": "tsx ingestLocalRecipes.ts",
    "extract:canonical": "tsx extractCanonicalFromCSV.ts",
    "seed:canonical": "tsx seedCanonicalItems.ts",
    "setup:free": "npm run extract:canonical && npm run ingest:local",
    "setup:manual": "npm run seed:canonical && npm run ingest:local",
    "clean:ingredients": "tsx cleanIngredients.ts",
    "clean:ingredients:edge": "tsx cleanIngredientsViaEdge.ts",
    "link:pantry": "tsx linkPantryItems.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@supabase/supabase-js": "^2.58.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
